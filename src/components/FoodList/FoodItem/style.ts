import styled from 'styled-components'

const Wrapper = styled.div`
  width: 15rem;
  padding: 1rem 1rem 0;
  margin-top: 0.5rem;
  .food-image {
    border-radius: 10px;
  }
`

export default Wrapper
