import styled from 'styled-components'

const Wrapper = styled.div`
  width: 15rem;
  padding: 1rem 1rem 0;
  margin-top: 0.5rem;
  font-size: 0.8rem;
  text-align: center;
  .food-image {
    border-radius: 10px;
  }
  .food-name {
    margin-top: 0.5rem;
  }
`

export default Wrapper
